<%= form_for(barrel_status) do |f| %>
  <% if barrel_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(barrel_status.errors.count, "error") %> prohibited this barrel_status from being saved:</h2>

      <ul>
      <% barrel_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :temperature %>
    <%= f.text_field :temperature %>
  </div>

  <div class="field">
    <%= f.label :temperature_tstmp %>
    <%= f.datetime_select :temperature_tstmp %>
  </div>

  <div class="field">
    <%= f.label :state %>
    <%= f.text_field :state %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
